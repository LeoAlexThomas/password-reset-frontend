import React from "react";

const FormTitle = ({ title }) => {
  return (
    <h3 className="font-Title text-2xl sm:text-4xl font-bold text-center">
      {title}
    </h3>
  );
};

export default FormTitle;
